import React, { useState } from 'react'; import { Search, ShieldAlert, CheckCircle2, XCircle, ChevronRight, RotateCcw, AlertTriangle, BookOpen, Building2 } from 'lucide-react'; // --- DATOS DEL JUEGO --- const SUSPECTS = [ { id: 's1', name: 'Don Derroche', desc: 'Adquiere productos sin evaluar su necesidad real.' }, { id: 's2', name: 'Madame Plástico', desc: 'Prioriza sistemáticamente los envases de un solo uso.' }, { id: 's3', name: 'Dr. Obsolescencia', desc: 'Desecha dispositivos electrónicos reparables.' }, { id: 's4', name: 'Capitán Fast-Fashion', desc: 'Consume moda rápida de forma compulsiva.' } ]; const ACTIONS = [ { id: 'a1', name: 'Ausencia de reciclaje', desc: 'Desecho de envases en el contenedor de restos.' }, { id: 'a2', name: 'Desperdicio alimentario', desc: 'Omisión del consumo de alimentos antes de su caducidad.' }, { id: 'a3', name: 'Derroche energético', desc: 'Uso ineficiente de recursos hídricos y eléctricos.' }, { id: 'a4', name: 'Gestión tóxica', desc: 'Eliminación de baterías fuera del Punto Limpio.' } ]; const LOCATIONS = [ { id: 'l1', name: 'El Supermercado', desc: 'Punto de abastecimiento de bienes de consumo.' }, { id: 'l2', name: 'El Centro Comercial', desc: 'Área de gran concentración de establecimientos.' }, { id: 'l3', name: 'La Cocina Residencial', desc: 'Núcleo del consumo y gestión de residuos domésticos.' }, { id: 'l4', name: 'La Superficie de Electrónica', desc: 'Distribuidor de aparatos tecnológicos.' } ]; const QUESTIONS = [ { q: 'Según las directrices de reciclaje, ¿qué contenedor debe utilizarse para la correcta gestión de envases de plástico, latas y briks?', options: ['Contenedor Amarillo', 'Contenedor Azul', 'Contenedor Verde'], correct: 0 }, { q: 'El principio de las 3R, fundamental en las políticas de consumo responsable, hace referencia a:', options: ['Rentabilizar, Recuperar, Repartir', 'Reducir, Reutilizar, Reciclar', 'Recoger, Romper, Reparar'], correct: 1 }, { q: 'Para garantizar la seguridad medioambiental, ¿dónde deben depositarse los aparatos eléctricos y electrónicos (RAEE) que han finalizado su vida útil?', options: ['En el contenedor de fracción resto (gris)', 'En cualquier papelera urbana', 'En el Punto Limpio municipal'], correct: 2 }, { q: 'El término "Greenwashing" (ecopostureo o lavado verde) define una práctica donde:', options: ['Las empresas realizan un marketing engañoso para parecer más ecológicas de lo que son.', 'Se utilizan detergentes biodegradables para el lavado de vehículos.', 'Se fomenta la correcta separación de envases de vidrio.'], correct: 0 }, { q: 'La adquisición de productos de "Kilómetro 0" o de proximidad tiene como principal beneficio medioambiental:', options: ['La aceleración de la fecha de caducidad.', 'La drástica reducción de la huella de carbono asociada al transporte.', 'El aumento del uso de envases plásticos protectores.'], correct: 1 }, { q: 'En el etiquetado energético europeo, ¿qué clasificación indica la máxima eficiencia y el menor consumo eléctrico?', options: ['Clasificación G', 'Clasificación C', 'Clasificación A'], correct: 2 }, { q: 'Desde la perspectiva de la sostenibilidad, el mayor perjuicio del modelo "Fast Fashion" (moda rápida) es:', options: ['La generación masiva de residuos textiles y el alto impacto de su producción.', 'La excesiva durabilidad de las prendas en los armarios.', 'La dificultad técnica para reciclar los tejidos naturales.'], correct: 0 }, { q: 'Ante un excedente de comida en buen estado tras su elaboración, la práctica más acorde con el consumo responsable es:', options: ['Su eliminación directa como residuo orgánico.', 'Su conservación adecuada (refrigeración o congelación) para un consumo posterior.', 'Su exposición a temperatura ambiente hasta su deterioro.'], correct: 1 }, { q: 'Para reducir el impacto ambiental derivado del transporte de la compra, la acción más recomendable es:', options: ['Utilizar bolsas de tela reutilizables o carro de la compra.', 'Adquirir bolsas de plástico de un solo uso en cada visita.', 'Transportar los artículos individualmente sin embalaje auxiliar.'], correct: 0 } ]; export default function EcoCluedo() { // --- ESTADOS DEL JUEGO --- const [gameState, setGameState] = useState('start'); // start, playing, won, lost const [solution, setSolution] = useState({ suspect: null, action: null, location: null }); // Estado para la gestión de pistas const [givenClues, setGivenClues] = useState([]); // IDs de pistas que el juego ha dado (para no repetirlas) const [crossedOutItems, setCrossedOutItems] = useState([]); // IDs que el usuario tacha manualmente // Estados para el Quiz const [availableQuestions, setAvailableQuestions] = useState([]); const [currentQuestion, setCurrentQuestion] = useState(null); const [quizFeedback, setQuizFeedback] = useState(null); // 'correct', 'incorrect' const [lastClueGiven, setLastClueGiven] = useState(''); // Estados para la Acusación const [accusation, setAccusation] = useState({ suspect: '', action: '', location: '' }); const [accusationsLeft, setAccusationsLeft] = useState(3); const [accusationFeedback, setAccusationFeedback] = useState(''); // --- INICIALIZACIÓN --- const startGame = () => { // 1. Elegir la solución secreta al azar const secSuspect = SUSPECTS[Math.floor(Math.random() * SUSPECTS.length)]; const secAction = ACTIONS[Math.floor(Math.random() * ACTIONS.length)]; const secLocation = LOCATIONS[Math.floor(Math.random() * LOCATIONS.length)]; setSolution({ suspect: secSuspect.id, action: secAction.id, location: secLocation.id }); // 2. Barajar preguntas const shuffledQuestions = [...QUESTIONS].sort(() => 0.5 - Math.random()); setAvailableQuestions(shuffledQuestions); setCurrentQuestion(shuffledQuestions[0]); // 3. Reiniciar estados setGivenClues([]); setCrossedOutItems([]); setAccusationsLeft(3); setGameState('playing'); setAccusation({ suspect: '', action: '', location: '' }); setAccusationFeedback(''); setLastClueGiven(''); setQuizFeedback(null); }; // --- LÓGICA DEL QUIZ Y PISTAS --- const handleAnswer = (selectedIndex) => { if (selectedIndex === currentQuestion.correct) { setQuizFeedback('correct'); revealClue(); } else { setQuizFeedback('incorrect'); setLastClueGiven('Respuesta incorrecta. No se ha obtenido información concluyente en esta consulta.'); } // NOTA: Se elimina el setTimeout para que el usuario pueda leer la pista con calma }; const proceedToNextQuestion = () => { setQuizFeedback(null); const nextQs = availableQuestions.slice(1); if (nextQs.length > 0) { setAvailableQuestions(nextQs); setCurrentQuestion(nextQs[0]); } else { // Si se acaban las preguntas, barajamos de nuevo const shuffledQuestions = [...QUESTIONS].sort(() => 0.5 - Math.random()); setAvailableQuestions(shuffledQuestions); setCurrentQuestion(shuffledQuestions[0]); } }; const revealClue = () => { const allItems = [ ...SUSPECTS.map(s => ({ ...s, type: 'sospechoso' })), ...ACTIONS.map(a => ({ ...a, type: 'acción' })), ...LOCATIONS.map(l => ({ ...l, type: 'lugar' })) ]; // Buscar pistas que NO sean la solución y que NO se hayan dado todavía const possibleClues = allItems.filter(item => item.id !== solution.suspect && item.id !== solution.action && item.id !== solution.location && !givenClues.includes(item.id) ); if (possibleClues.length > 0) { const randomClue = possibleClues[Math.floor(Math.random() * possibleClues.length)]; setGivenClues(prev => [...prev, randomClue.id]); let mensaje = ''; if (randomClue.type === 'sospechoso') mensaje = Las pruebas periciales descartan la participación de: ${randomClue.name}.; if (randomClue.type === 'acción') mensaje = La auditoría confirma que la infracción cometida NO fue: "${randomClue.name}".; if (randomClue.type === 'lugar') mensaje = Los registros sitúan el incidente fuera de las instalaciones de: ${randomClue.name}.; setLastClueGiven(${mensaje} (No olvide tacharlo en su registro).); } else { setLastClueGiven("Se han agotado las vías de investigación pericial. Dispone de toda la información posible para formular la acusación."); } }; // --- LÓGICA DE LA LIBRETA MANUAL --- const toggleCrossOut = (id) => { setCrossedOutItems(prev => prev.includes(id) ? prev.filter(itemId => itemId !== id) : [...prev, id] ); }; // --- LÓGICA DE ACUSACIÓN --- const makeAccusation = (e) => { e.preventDefault(); if (!accusation.suspect || !accusation.action || !accusation.location) { setAccusationFeedback('Es imperativo rellenar todos los campos del formulario de acusación oficial.'); return; } if ( accusation.suspect === solution.suspect && accusation.action === solution.action && accusation.location === solution.location ) { setGameState('won'); } else { const newAttempts = accusationsLeft - 1; setAccusationsLeft(newAttempts); if (newAttempts <= 0) { setGameState('lost'); } else { setAccusationFeedback(Acusación desestimada. Las pruebas no coinciden. Intentos restantes: ${newAttempts}.); } } }; // --- IMAGEN DE FONDO REALISTA --- const backgroundStyle = { backgroundImage: "url('https://images.unsplash.com/photo-1497366216548-37526070297c?auto=format&fit=crop&q=80')", backgroundSize: 'cover', backgroundPosition: 'center', backgroundAttachment: 'fixed', }; // --- RENDERIZADO CONDICIONAL --- if (gameState === 'start') { return ( <div style={backgroundStyle} className="min-h-screen flex items-center justify-center p-4 font-sans text-[#1a365d]"> <div className="absolute inset-0 bg-[#0f284e]/70 backdrop-blur-sm"></div> <div className="max-w-4xl bg-white rounded-xl shadow-2xl overflow-hidden relative z-10 border-t-8 border-[#3b6baf]"> <div className="bg-[#f8fafc] p-6 text-center border-b border-gray-200"> <img src="Escuela-de-Consumo.png" alt="Escuela de Consumo Ayuntamiento de Salamanca" className="mx-auto h-24 object-contain mb-4" onError={(e) => { e.target.style.display = 'none'; e.target.parentElement.innerHTML += '<div class="text-2xl font-bold text-[#3b6baf]">Escuela de Consumo<br/><span class="text-lg text-gray-600">Ayuntamiento de Salamanca</span></div>'; }} /> <h1 className="text-3xl font-bold mb-2 tracking-tight text-[#1e3a8a]">Expediente de Infracción: El Gran Desperdicio</h1> <p className="text-lg font-medium text-gray-600">Programa de Formación en Consumo Responsable</p> </div> <div className="p-8 text-base leading-relaxed text-gray-700 max-h-[60vh] overflow-y-auto"> <p className="mb-6"> <strong>Estimado/a participante:</strong><br/><br/> Se ha registrado una grave infracción contra las normativas de consumo responsable y sostenibilidad en nuestro municipio. Como agente inspector de la Escuela de Consumo, su misión es esclarecer los hechos utilizando la deducción y sus conocimientos. </p> <h3 className="font-bold text-[#1e3a8a] text-lg border-b-2 border-[#1e3a8a] mb-4 pb-1">Metodología de la Investigación</h3> <p className="mb-6"> Para obtener acceso a las pruebas del caso, deberá superar los controles de conocimiento (preguntas). <strong>Cada respuesta correcta le proporcionará una pista fiable que descarta una opción.</strong> Tenga en cuenta que <strong>deberá tachar manualmente</strong> en su bloc de notas a los sospechosos, acciones y lugares descartados haciendo clic sobre ellos. Dispone de 3 intentos para formular el acta de acusación. </p> <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8"> <div className="bg-blue-50 p-4 rounded-lg border border-blue-100"> <h4 className="font-bold text-[#1e3a8a] mb-2">1. Los Sospechosos</h4> <ul className="text-sm space-y-2"> {SUSPECTS.map(s => <li key={s.id}><strong>{s.name}</strong>: {s.desc}</li>)} </ul> </div> <div className="bg-blue-50 p-4 rounded-lg border border-blue-100"> <h4 className="font-bold text-[#1e3a8a] mb-2">2. Las Infracciones</h4> <ul className="text-sm space-y-2"> {ACTIONS.map(a => <li key={a.id}><strong>{a.name}</strong>: {a.desc}</li>)} </ul> </div> <div className="bg-blue-50 p-4 rounded-lg border border-blue-100"> <h4 className="font-bold text-[#1e3a8a] mb-2">3. Las Localizaciones</h4> <ul className="text-sm space-y-2"> {LOCATIONS.map(l => <li key={l.id}><strong>{l.name}</strong>: {l.desc}</li>)} </ul> </div> </div> <button onClick={startGame} className="w-full bg-[#1e3a8a] hover:bg-[#1e40af] text-white font-bold py-4 px-8 rounded-lg transition-all shadow-lg flex justify-center items-center gap-2 text-lg" > Iniciar Expediente de Investigación <ChevronRight className="w-6 h-6" /> </button> </div> </div> </div> ); } if (gameState === 'won' || gameState === 'lost') { const isWin = gameState === 'won'; const solSuspect = SUSPECTS.find(s => s.id === solution.suspect).name; const solAction = ACTIONS.find(a => a.id === solution.action).name; const solLocation = LOCATIONS.find(l => l.id === solution.location).name; return ( <div style={backgroundStyle} className="min-h-screen flex items-center justify-center p-4"> <div className="absolute inset-0 bg-[#0f284e]/80 backdrop-blur-md"></div> <div className="max-w-2xl w-full bg-white rounded-xl shadow-2xl overflow-hidden text-center relative z-10 border-2 border-[#1e3a8a]"> <div className={p-8 ${isWin ? 'bg-[#1e40af]' : 'bg-gray-800'} text-white border-b-4 ${isWin ? 'border-[#3b82f6]' : 'border-red-500'}}> <Building2 className="w-16 h-16 mx-auto mb-4 opacity-80" /> <h1 className="text-3xl font-bold mb-2"> {isWin ? 'Resolución Oficial del Caso' : 'Cierre de Expediente (Inconcluso)'} </h1> <p className="text-sm font-medium opacity-90 uppercase tracking-widest">Escuela de Consumo · Ayuntamiento de Salamanca</p> </div> <div className="p-10"> <p className="text-xl mb-6 text-gray-800 font-medium"> {isWin ? 'El expediente ha sido instruido con éxito. Gracias a su labor deductiva, hemos identificado al responsable y evitado mayores daños al medio ambiente.' : 'Se han agotado los intentos procesales legales. El responsable ha logrado eludir a las autoridades en esta ocasión.'} </p> <div className="bg-[#f1f5f9] border border-gray-300 rounded-lg p-6 mb-8 inline-block text-left shadow-inner w-full max-w-md"> <h3 className="font-bold text-[#1e3a8a] uppercase tracking-wide text-sm mb-4 border-b-2 border-gray-200 pb-2 flex items-center gap-2"> <ShieldAlert className="w-4 h-4"/> Hechos Probados </h3> <p className="mb-3 text-gray-700"><span className="font-bold text-[#0f172a]">Infractor Identificado:</span> <br/>{solSuspect}</p> <p className="mb-3 text-gray-700"><span className="font-bold text-[#0f172a]">Infracción Cometida:</span> <br/>{solAction}</p> <p className="text-gray-700"><span className="font-bold text-[#0f172a]">Lugar de los Hechos:</span> <br/>{solLocation}</p> </div> <button onClick={startGame} className="w-full max-w-md mx-auto bg-[#1e3a8a] hover:bg-[#1e40af] text-white font-bold py-3 px-6 rounded-lg transition-colors flex justify-center items-center gap-2" > <RotateCcw className="w-5 h-5" /> Iniciar Nueva Investigación </button> </div> </div> </div> ); } // --- RENDERIZADO PRINCIPAL (Jugando) --- return ( <div style={backgroundStyle} className="min-h-screen font-sans text-gray-800 p-4 md:p-8"> <div className="absolute inset-0 bg-[#f8fafc]/90 backdrop-blur-[2px] z-0 fixed"></div> <div className="max-w-6xl mx-auto relative z-10"> {/* CABECERA CORPORATIVA */} <header className="flex flex-col md:flex-row justify-between items-center mb-8 bg-white p-4 md:p-6 rounded-xl shadow-lg border-t-4 border-[#1e3a8a]"> <div className="flex items-center gap-4 mb-4 md:mb-0 w-full md:w-auto"> <img src="Escuela-de-Consumo.png" alt="Logo" className="h-16 object-contain hidden sm:block" onError={(e) => e.target.style.display='none'} /> <div className="border-l-2 border-gray-200 pl-4"> <h1 className="text-xl md:text-2xl font-bold text-[#1e3a8a] leading-tight">Escuela de Consumo</h1> <p className="text-sm font-medium text-gray-500">Ayuntamiento de Salamanca | Panel de Investigación</p> </div> </div> <div className="flex flex-col items-end"> <span className="text-xs font-bold text-gray-400 uppercase tracking-wider mb-1">Intentos de Acusación</span> <div className="flex gap-2"> {[...Array(3)].map((_, i) => ( <div key={i} className={w-5 h-5 rounded border ${i < accusationsLeft ? 'bg-[#3b82f6] border-[#1d4ed8] shadow-inner' : 'bg-gray-100 border-gray-300'}} /> ))} </div> </div> </header> <div className="grid grid-cols-1 lg:grid-cols-12 gap-8"> {/* COLUMNA IZQUIERDA: LIBRETA DE DETECTIVE (AHORA MANUAL) */} <div className="lg:col-span-4 space-y-6"> <div className="bg-white p-6 rounded-xl shadow-lg border border-gray-200"> <h2 className="text-lg font-bold mb-4 text-[#1e3a8a] flex items-center gap-2 border-b-2 border-gray-100 pb-2"> <BookOpen className="w-5 h-5 text-[#3b82f6]"/> Registro de Oficial </h2> <p className="text-xs text-gray-500 mb-6 bg-gray-50 p-2 rounded"> <strong>Instrucción:</strong> Haz clic sobre las opciones para tacharlas o destacharlas a medida que reúnas pruebas. </p> {/* Sospechosos */} <div className="mb-6"> <h3 className="font-bold text-gray-800 mb-2 bg-blue-50 p-1 px-2 rounded">Individuos Investigados</h3> <ul className="space-y-1 pl-2"> {SUSPECTS.map(s => { const isCrossed = crossedOutItems.includes(s.id); return ( <li key={s.id} onClick={() => toggleCrossOut(s.id)} className={flex items-start text-sm p-1 rounded cursor-pointer transition-colors hover:bg-gray-100 ${isCrossed ? 'text-gray-400 line-through' : 'text-gray-700'}} > <span className={mr-2 font-bold ${isCrossed ? 'text-red-400' : 'text-[#3b82f6]'}}>{isCrossed ? '✗' : '·'}</span> <span>{s.name}</span> </li> ); })} </ul> </div> {/* Acciones */} <div className="mb-6"> <h3 className="font-bold text-gray-800 mb-2 bg-blue-50 p-1 px-2 rounded">Posibles Infracciones</h3> <ul className="space-y-1 pl-2"> {ACTIONS.map(a => { const isCrossed = crossedOutItems.includes(a.id); return ( <li key={a.id} onClick={() => toggleCrossOut(a.id)} className={flex items-start text-sm p-1 rounded cursor-pointer transition-colors hover:bg-gray-100 ${isCrossed ? 'text-gray-400 line-through' : 'text-gray-700'}} > <span className={mr-2 font-bold ${isCrossed ? 'text-red-400' : 'text-[#3b82f6]'}}>{isCrossed ? '✗' : '·'}</span> <span>{a.name}</span> </li> ); })} </ul> </div> {/* Lugares */} <div> <h3 className="font-bold text-gray-800 mb-2 bg-blue-50 p-1 px-2 rounded">Escenarios del Incidente</h3> <ul className="space-y-1 pl-2"> {LOCATIONS.map(l => { const isCrossed = crossedOutItems.includes(l.id); return ( <li key={l.id} onClick={() => toggleCrossOut(l.id)} className={flex items-start text-sm p-1 rounded cursor-pointer transition-colors hover:bg-gray-100 ${isCrossed ? 'text-gray-400 line-through' : 'text-gray-700'}} > <span className={mr-2 font-bold ${isCrossed ? 'text-red-400' : 'text-[#3b82f6]'}}>{isCrossed ? '✗' : '·'}</span> <span>{l.name}</span> </li> ); })} </ul> </div> </div> </div> {/* COLUMNA DERECHA: ACCIÓN (QUIZ Y ACUSACIÓN) */} <div className="lg:col-span-8 space-y-6"> {/* SECCIÓN DE QUIZ */} <div className="bg-white p-6 md:p-8 rounded-xl shadow-lg border border-gray-200"> <h2 className="text-xl font-bold mb-6 flex items-center gap-2 text-[#1e3a8a] border-b-2 border-gray-100 pb-3"> <Search className="w-6 h-6"/> Evaluación de Conocimientos (Obtención de Pruebas) </h2> {currentQuestion && !quizFeedback ? ( <div className="animate-fade-in"> <p className="text-gray-800 font-semibold mb-6 text-lg bg-blue-50/50 p-4 rounded-lg border-l-4 border-[#3b82f6]"> {currentQuestion.q} </p> <div className="space-y-3"> {currentQuestion.options.map((opt, index) => ( <button key={index} onClick={() => handleAnswer(index)} className="w-full text-left p-4 rounded-lg border border-gray-200 hover:border-[#3b82f6] hover:bg-blue-50 transition-all text-gray-700 font-medium shadow-sm hover:shadow" > {opt} </button> ))} </div> </div> ) : ( <div className={p-8 rounded-xl border ${quizFeedback === 'correct' ? 'bg-green-50 border-green-200' : 'bg-red-50 border-red-200'}}> {quizFeedback === 'correct' ? ( <div className="flex flex-col items-center"> <CheckCircle2 className="w-16 h-16 mb-4 text-green-600" /> <h3 className="font-bold text-2xl mb-2 text-green-800">Evaluación Superada</h3> <div className="bg-white p-4 rounded-lg border border-green-100 shadow-sm w-full text-center my-4"> <p className="text-gray-800 font-medium text-lg leading-relaxed">{lastClueGiven}</p> </div> <button onClick={proceedToNextQuestion} className="mt-2 bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg transition-colors flex items-center gap-2" > Siguiente Evaluación <ChevronRight className="w-5 h-5"/> </button> </div> ) : ( <div className="flex flex-col items-center"> <XCircle className="w-16 h-16 mb-4 text-red-600" /> <h3 className="font-bold text-2xl mb-2 text-red-800">Evaluación Fallida</h3> <p className="text-gray-700 text-center my-4">{lastClueGiven}</p> <button onClick={proceedToNextQuestion} className="mt-2 bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-6 rounded-lg transition-colors flex items-center gap-2" > Intentar Nueva Pregunta <ChevronRight className="w-5 h-5"/> </button> </div> )} </div> )} </div> {/* SECCIÓN DE ACUSACIÓN FORMAL */} <div className="bg-[#0f172a] text-white p-6 md:p-8 rounded-xl shadow-lg border-t-4 border-[#3b82f6]"> <h2 className="text-xl font-bold mb-2 flex items-center gap-2 text-white"> <AlertTriangle className="w-5 h-5 text-yellow-400"/> Formulario Oficial de Acusación </h2> <p className="text-sm text-gray-400 mb-6"> Advertencia: Una acusación infundada restará un intento del expediente. Revise sus anotaciones antes de proceder. </p> <form onSubmit={makeAccusation} className="space-y-6"> <div className="grid grid-cols-1 md:grid-cols-3 gap-4"> <div> <label className="block text-xs font-bold text-gray-400 uppercase mb-2">Sujeto Infractor</label> <select className="w-full bg-[#1e293b] border border-gray-600 rounded-lg p-3 text-white focus:ring-2 focus:ring-[#3b82f6] focus:border-transparent outline-none" value={accusation.suspect} onChange={(e) => setAccusation({ ...accusation, suspect: e.target.value })} > <option value="">Seleccione...</option> {SUSPECTS.map(s => <option key={s.id} value={s.id}>{s.name}</option>)} </select> </div> <div> <label className="block text-xs font-bold text-gray-400 uppercase mb-2">Tipo de Infracción</label> <select className="w-full bg-[#1e293b] border border-gray-600 rounded-lg p-3 text-white focus:ring-2 focus:ring-[#3b82f6] focus:border-transparent outline-none" value={accusation.action} onChange={(e) => setAccusation({ ...accusation, action: e.target.value })} > <option value="">Seleccione...</option> {ACTIONS.map(a => <option key={a.id} value={a.id}>{a.name}</option>)} </select> </div> <div> <label className="block text-xs font-bold text-gray-400 uppercase mb-2">Lugar de los Hechos</label> <select className="w-full bg-[#1e293b] border border-gray-600 rounded-lg p-3 text-white focus:ring-2 focus:ring-[#3b82f6] focus:border-transparent outline-none" value={accusation.location} onChange={(e) => setAccusation({ ...accusation, location: e.target.value })} > <option value="">Seleccione...</option> {LOCATIONS.map(l => <option key={l.id} value={l.id}>{l.name}</option>)} </select> </div> </div> {accusationFeedback && ( <div className="bg-red-500/20 border border-red-500 text-red-200 p-4 rounded-lg text-sm text-center font-medium"> {accusationFeedback} </div> )} <button type="submit" className="w-full bg-[#3b82f6] hover:bg-[#2563eb] text-white font-bold py-4 px-4 rounded-lg transition-colors mt-2 text-lg uppercase tracking-wide" > Emitir Veredicto </button> </form> </div> </div> </div> </div> </div> ); }
